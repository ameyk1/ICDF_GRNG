


// Instantiate the module
ROM_coef instance_name (
    .clk(clk), 
    .rst(rst), 
    .en_coef(en_coef), 
    .segment(segment), 
    .coef0(coef0), 
    .coef1(coef1), 
    .coef2(coef2)
    );


